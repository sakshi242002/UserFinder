import React from 'react';
import { LoadingWrapper, Loading } from './LoadingSpinnerElements';

export const LoadingSpinner = () => {
  return (
    <LoadingWrapper>
      <Loading></Loading>
    </LoadingWrapper>
  );
};
